// Stub so tooling (e.g. the shadcn CLI) detects this as a Vite project.
// The real build config is electron.vite.config.ts — keep changes there.
import { defineConfig } from 'vite'

export default defineConfig({})
